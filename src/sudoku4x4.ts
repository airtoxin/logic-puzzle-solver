import * as Logic from "logic-solver";
import { range } from "./helpers";

const size = 4;

const board = range(size).map(y => range(size).map(x => `${x}-${y}`));

for (const row of board) {
  for (const cell of row) {
  }
}
